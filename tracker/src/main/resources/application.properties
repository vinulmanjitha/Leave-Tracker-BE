spring.application.name=tracker

# ===============================
# Server
# ===============================
server.port=8080

# ===============================
# PostgreSQL Connection (AWS RDS)
# ===============================
spring.datasource.url=jdbc:postgresql://leave-tracker-db.cdo0iic6acih.eu-north-1.rds.amazonaws.com:5432/leave_tracker
spring.datasource.username=postgres
spring.datasource.password=Diplo6838

# Optional: show SQL in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Hibernate / JPA Settings
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# CORS
# ===============================
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
spring.web.cors.allowed-headers=*
